# Story 1.6: Theme Variant Generation

Status: ready-for-dev

## Story

As a **solo developer**,
I want light and dark theme variants generated automatically from my palette,
So that I support both appearances without designing two separate color systems.

## Acceptance Criteria

1. **Given** the user selected "yes" to light/dark theme generation (from Story 1.2), **When** the theme generation step runs, **Then** a light theme is generated with appropriate semantic mappings (light backgrounds, dark text).
2. **Given** a light theme is generated, **When** dark theme generation runs, **Then** a dark theme is generated by inverting the semantic mappings (dark backgrounds, light text).
3. **Given** both themes are generated, **When** the theme tokens are examined, **Then** both themes reference the same primitive palette — only the semantic mappings differ.
4. **Given** the user selected "no" to themes (from Story 1.2), **When** the theme step runs, **Then** only a single default theme is generated (the light mapping from Story 1.5 becomes the sole theme).
5. **Given** themes are generated, **When** the output structure is prepared, **Then** the theme structure follows Style Dictionary's platform/theme conventions for later processing in Story 1.8.

## Tasks / Subtasks

- [ ] Task 1: Design the theme data model (AC: #1, #2, #3, #5)
  - [ ] 1.1: Define `Theme` type in `src/types/tokens.ts`: `{ name: string; semanticTokens: SemanticToken[] }` — each theme is a named set of semantic-to-primitive mappings
  - [ ] 1.2: Define `ThemeCollection` type: `{ primitives: PrimitiveToken[]; themes: Theme[] }`
  - [ ] 1.3: The primitives are shared across all themes; themes only vary in semantic mappings
- [ ] Task 2: Create `src/generators/themes.ts` — theme variant generator (AC: #1, #2, #3)
  - [ ] 2.1: Implement `generateLightTheme(semanticTokens: SemanticToken[]): Theme` — wraps the existing semantic mapping (from Story 1.5) as the "light" named theme
  - [ ] 2.2: Implement `generateDarkTheme(semanticTokens: SemanticToken[], primitives: PrimitiveToken[]): Theme` — creates an inverted mapping:
    - Background tokens: swap to dark primitive steps (e.g., `color.neutral.50` → `color.neutral.900`)
    - Content tokens: swap to light primitive steps (e.g., `color.neutral.900` → `color.neutral.50`)
    - Border tokens: adjust to appropriate dark-mode contrast steps
    - Primary/accent backgrounds: may use lighter/more vibrant steps for dark mode
    - Spacing and typography semantics: remain unchanged across themes
  - [ ] 2.3: Implement `generateThemes(semanticTokens, primitives, enableDarkMode: boolean): ThemeCollection`
- [ ] Task 3: Define the dark-mode inversion strategy (AC: #2, #3)
  - [ ] 3.1: Create an inversion mapping that knows how to flip primitive references:
    - For neutral ramps: reverse the step order (50↔950, 100↔900, 200↔800, etc.)
    - For primary/accent ramps: shift steps to maintain contrast on dark backgrounds
    - For non-color tokens (spacing, typography): no change between themes
  - [ ] 3.2: The inversion must preserve DTCG reference syntax — dark theme tokens still reference the same primitives, just different steps
- [ ] Task 4: Structure themes for Style Dictionary compatibility (AC: #5)
  - [ ] 4.1: Research Style Dictionary v5 theme/platform conventions for multi-theme output
  - [ ] 4.2: Structure the `ThemeCollection` data so it maps cleanly to separate JSON source files per theme or a theme-scoped directory structure (e.g., `tokens/semantic/light/`, `tokens/semantic/dark/`)
  - [ ] 4.3: Ensure theme names are valid identifiers for CSS class selectors (e.g., `.theme-light`, `.theme-dark` or `[data-theme="light"]`)
- [ ] Task 5: Implement progress narrative and integrate into pipeline (AC: #1, #2, #4)
  - [ ] 5.1: If user opted for themes: narrate "Generating light theme..." and "Generating dark theme..." with token counts
  - [ ] 5.2: If user opted out of themes: narrate "Using default theme (light)" with a brief note that themes can be enabled later
  - [ ] 5.3: Wire into init pipeline after Story 1.5's semantic mapping
  - [ ] 5.4: Pass `ThemeCollection` forward for preview (Story 1.7) and output (Story 1.8)

## Dev Notes

### Technical Stack

| Concern | Choice | Version |
|---------|--------|---------|
| Runtime | Node.js | >=18 (LTS) |
| Language | TypeScript | ^5.x |
| Build | tsup | ^8.x |
| CLI prompts | @clack/prompts | ^1.2.0 |
| Output engine | Style Dictionary | ^5.x (not installed yet — needed for understanding theme conventions) |

### Architecture Constraints

- **ESM-only** — all imports use ESM syntax.
- **Themes are remappings, not regenerations:** Both light and dark themes use the SAME primitive palette. Only the semantic token `$value` references change. This is the core architectural principle.
- **Style Dictionary v5 is the target consumer:** The theme data model must be structured for Style Dictionary to process in Story 1.8. Research SD v5's multi-theme patterns BEFORE finalizing the data model.
- **Clack for all output** — `log.step()` / `log.info()` for narrative.

### Theme Architecture

The in-memory structure after this story completes:

```
ThemeCollection {
  primitives: PrimitiveToken[]          // Shared — color, spacing, typography
  themes: [
    {
      name: "light",
      semanticTokens: SemanticToken[]   // Same as Story 1.5 output
    },
    {
      name: "dark",
      semanticTokens: SemanticToken[]   // Inverted color mappings, same spacing/typo
    }
  ]
}
```

If the user chose no themes, the collection has a single theme named "default" with the standard mappings.

### Dark Mode Inversion Strategy

The inversion is a **ramp step flip**, not a color value recalculation:

| Semantic Role | Light Theme Ref | Dark Theme Ref |
|--------------|-----------------|----------------|
| `color.background.default` | `color.neutral.50` (lightest) | `color.neutral.900` (darkest) |
| `color.background.primary` | `color.blue.500` | `color.blue.400` (slightly lighter for dark bg contrast) |
| `color.content.default` | `color.neutral.900` (darkest) | `color.neutral.50` (lightest) |
| `color.content.primary` | `color.blue.700` | `color.blue.300` |
| `color.border.default` | `color.neutral.200` | `color.neutral.700` |

The exact step mapping depends on the ramp steps available from `@quieto/palettes`. The key principle: maintain sufficient contrast between background and content tokens in both themes.

### Style Dictionary v5 Theme Conventions

Style Dictionary v5 supports multiple approaches for theming:
1. **Separate source directories** per theme: `tokens/semantic/light/`, `tokens/semantic/dark/`
2. **Platform-level filtering** using `include`/`source` arrays per theme
3. **DTCG `$extensions`** with mode metadata

The recommended approach for this CLI: **separate source directories per theme** with shared primitives. This is the most straightforward for both SD processing and human readability:
```
tokens/
├── primitive/           # Shared across all themes
│   ├── color.json
│   ├── spacing.json
│   └── typography.json
├── semantic/
│   ├── light/           # Light theme semantic mappings
│   │   ├── color.json
│   │   ├── spacing.json
│   │   └── typography.json
│   └── dark/            # Dark theme semantic mappings
│       ├── color.json
│       ├── spacing.json
│       └── typography.json
```

### What NOT to Build

- Do NOT implement named themes beyond light/dark (that's a future feature from brainstorming #33)
- Do NOT implement theme switching UI in the preview (Story 1.7 handles preview display)
- Do NOT install Style Dictionary yet (Story 1.8)
- Do NOT write files to disk
- Do NOT recalculate colors — only remap references to existing primitives

### File Structure

```
src/
├── generators/
│   ├── color.ts                    # From Story 1.3
│   ├── spacing.ts                  # From Story 1.4
│   ├── typography.ts               # From Story 1.4
│   └── themes.ts                   # NEW — theme variant generation
├── mappers/
│   └── semantic.ts                 # From Story 1.5
├── types/
│   └── tokens.ts                   # MODIFIED — add Theme, ThemeCollection types
```

### Previous Story Intelligence

- **Semantic token output from 1.5:** Array of `SemanticToken[]` with DTCG references to primitives
- **Color ramp from 1.3:** Contains step numbers that can be inverted (50↔950, etc.)
- **Pipeline pattern:** Each step receives output from the previous step and passes enriched data forward
- **Clack narrative pattern:** `log.step()` for phase announcements, `log.info()` for details

### References

- [Source: docs/planning/epics.md#Story 1.6] — Acceptance criteria and story statement
- [Source: docs/brainstorming/brainstorming-session-2026-04-16-1200.md#Black Hat] — "Theme Variants in Quick-Start: Light/dark from a single yes/no question"
- [Source: docs/brainstorming/brainstorming-session-2026-04-16-1200.md#Blue Hat] — Phase 1 MVP includes light/dark themes
- [Source: Style Dictionary v5 docs] — Multi-theme platform/source conventions
- [Source: DTCG spec] — Reference syntax preserved across themes

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
