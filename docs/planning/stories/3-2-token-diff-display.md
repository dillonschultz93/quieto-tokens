# Story 3.2: Token Diff Display

Status: ready-for-dev

## Story

As a **solo developer making changes to my token system**,
I want to see exactly what will change before any files are written,
so that I can understand the impact of my modifications and catch unintended consequences.

## Story Scope Note

This story enhances the `quieto-tokens update` command (shipped in Story 3.1) with a **token diff display** that appears between the selective regeneration pipeline and the confirm/write step. Today, `update` runs `previewAndConfirm` which renders the entire final token system — the user sees all tokens but has no visibility into what actually changed vs. what was preserved from disk. This story adds a focused diff view that groups changes by tier and category, distinguishes added/modified/removed tokens, and traces cascading impacts (e.g., "Changing `color.blue.500` affects 12 semantic tokens").

**What this story IS:**
- A diff engine that compares the prior on-disk token state against the pipeline's regenerated collection.
- A diff renderer that displays changes in the terminal, grouped by tier (primitives → semantics), with color swatches for color token diffs and distinct markers for added/modified/removed tokens.
- A cascade summary that traces how primitive changes ripple into semantic tokens.
- Integration into the `update` command's flow, shown before the existing preview + override step.
- A "no changes detected" early-exit path when the regenerated tokens are identical to the on-disk tokens.

**What this story is NOT:**
- Not `--dry-run` (Story 3.3 — this story's diff only appears in the `update` flow, not `init`/`add`/`component`).
- Not changelog generation (Story 3.4 — the diff is a terminal-only display, not persisted).
- Not a modification to the `init` modify-flow — `init` continues to use the full preview without a diff.
- Not component-token diffing — component tokens are pure references and are not regenerated by `update` (Story 3.1 AC #19).

## Acceptance Criteria

### Diff computation

1. **Given** the `update` pipeline has produced a regenerated `ThemeCollection`, **When** the diff step runs, **Then** the prior token state is loaded from disk using `loadPrimitivesFromDisk` and `loadSemanticTokensFromDisk` (from Story 3.1's `src/pipeline/load-from-disk.ts`), and a token-level diff is computed.
2. **Given** the diff is computed, **When** tokens are compared, **Then** each token is matched by its canonical path (e.g., `color.blue.500`, `spacing.md`). A token present in the regenerated collection but absent on disk is **added**. A token present on disk but absent in the regenerated collection is **removed**. A token present in both but with a different `$value` is **modified**.
3. **Given** no tokens have changed (regenerated collection is identical to disk), **When** the diff is computed, **Then** the diff result is empty and the CLI reports "No changes to apply — your token system is up to date." via `p.log.info` and exits cleanly without proceeding to preview or writing files.

### Diff display — primitives

4. **Given** primitive tokens have changed, **When** the diff display renders, **Then** changes are grouped by category (e.g., "Color Primitives", "Spacing Primitives"), and within each category, grouped by subcategory where applicable (e.g., by hue for color tokens).
5. **Given** a color primitive was modified, **When** its diff line renders, **Then** both the old and new hex values are displayed with ANSI color swatches (where terminal supports it): e.g., `██ color.blue.500  #2563eb → ██ #1d4ed8`. Use `hexToAnsi` from `src/utils/color-display.ts`.
6. **Given** a non-color primitive was modified (spacing, typography), **When** its diff line renders, **Then** the old and new values are displayed inline: e.g., `spacing.md  16px → 24px`.
7. **Given** a primitive was added (e.g., a new hue ramp), **When** its diff line renders, **Then** it is prefixed with a `+` marker and the value is shown: e.g., `+ color.teal.500  #14b8a6`.
8. **Given** a primitive was removed (e.g., a hue ramp was dropped), **When** its diff line renders, **Then** it is prefixed with a `−` marker and the old value is shown: e.g., `− color.teal.500  #14b8a6`.

### Diff display — semantics

9. **Given** semantic tokens have changed, **When** the diff display renders, **Then** changes are grouped by theme (e.g., "Semantic Changes (Default)", "Semantic Changes (Light)", "Semantic Changes (Dark)"), and within each theme, grouped by category.
10. **Given** a semantic token's `$value` reference changed (e.g., `{color.blue.500}` → `{color.blue.600}`), **When** its diff line renders, **Then** both the old and new reference are shown: e.g., `color.background.primary  → {color.blue.500} → {color.blue.600}`. For color semantics, include swatches resolved from the primitive palette.
11. **Given** a semantic token was added or removed, **When** its diff line renders, **Then** the same `+`/`−` marker convention from primitives applies.

### Cascade tracing

12. **Given** one or more primitive tokens changed, **When** the diff step completes, **Then** a cascade summary is displayed showing how many semantic tokens were affected: e.g., "Changing 3 color primitives affects 12 semantic tokens across 2 themes." The summary appears after the detailed diff, before the preview.
13. **Given** a primitive was removed AND semantic tokens still reference it, **When** the cascade summary renders, **Then** the affected semantic tokens are called out as broken references: e.g., "⚠ color.background.accent still references removed primitive {color.teal.500}". (This is separate from Story 3.1's override conflict resolution, which handles user overrides — this catches mapper-default references that became stale.)

### User flow integration

14. **Given** the diff has been displayed, **When** the user is prompted, **Then** they see a `p.select` with options: "Accept changes and write", "Review full token preview" (enters existing `previewAndConfirm`), "Go back and modify further" (returns to category picker), or "Cancel" (exit without writing).
15. **Given** the user selects "Accept changes and write", **When** the write step runs, **Then** the output is written via `runOutputGeneration` with the scoped `WriteScope` (same as Story 3.1 — only modified categories' files are written). The existing preview step is skipped.
16. **Given** the user selects "Review full token preview", **When** the preview runs, **Then** the existing `previewAndConfirm` flow is entered (showing all tokens, offering the override loop). After the preview, the user can accept or cancel.
17. **Given** the user selects "Go back and modify further", **When** the flow resumes, **Then** the category picker from Story 3.1's `collectUpdateInputs` is re-entered. The user can select additional categories to modify. After making changes, the pipeline re-runs and a new diff is displayed.
18. **Given** the user selects "Cancel", **When** the cancel is processed, **Then** no files are written and the config is unchanged.

### Edge cases

19. **Given** the user runs `update` and makes changes that result in the exact same token values (e.g., re-enters the same brand color hex), **When** the diff is computed, **Then** the diff is empty and the "No changes to apply" early-exit activates (AC #3).
20. **Given** add-on categories exist on disk (e.g., shadow, border) but were not modified, **When** the diff displays, **Then** those categories are not shown in the diff at all — only modified categories appear.
21. **Given** component tokens exist, **When** the diff displays, **Then** component tokens are not diffed (they are not regenerated by `update`). A note is shown: "Component tokens are unchanged — they reference semantic tokens and will inherit your changes via the CSS cascade."

## Tasks / Subtasks

- [ ] **Task 1: Diff engine — `computeTokenDiff` (AC: #1, #2, #3)**
  - [ ] 1.1: Create `src/ui/diff.ts` exporting:
    ```typescript
    export interface TokenChange {
      kind: "added" | "removed" | "modified";
      path: string[];
      name: string;
      category: string;
      $type: string;
      oldValue?: string;
      newValue?: string;
    }

    export interface TokenDiff {
      primitiveChanges: TokenChange[];
      semanticChanges: Map<string, TokenChange[]>; // theme name → changes
      isEmpty: boolean;
    }

    export function computeTokenDiff(
      prior: ThemeCollection,
      current: ThemeCollection,
    ): TokenDiff;
    ```
  - [ ] 1.2: Compare primitives by building a `Map<string, PrimitiveToken>` keyed by `name` (the dot-joined path) for both prior and current. Walk both maps to determine added/removed/modified.
  - [ ] 1.3: Compare semantic tokens per theme — same map-keyed-by-name approach. Match themes by `theme.name`.
  - [ ] 1.4: Set `isEmpty` to `true` when all change arrays are empty.
  - [ ] 1.5: Keep the engine pure — no Clack I/O, no filesystem access. It takes two `ThemeCollection`s and returns a `TokenDiff`.

- [ ] **Task 2: Load prior state from disk (AC: #1)**
  - [ ] 2.1: Create `src/pipeline/diff-loader.ts` exporting `loadPriorCollection(config: QuietoConfig, cwd: string): Promise<ThemeCollection>`. This composes:
    - `loadPrimitivesFromDisk(cwd, tokensDir, config.categories)` (all categories — not just modified).
    - `loadSemanticTokensFromDisk(cwd, tokensDir, themeNames, config.categories)`.
    - `loadComponentTokensFromDisk(cwd, tokensDir)` (for completeness, though components are not diffed).
    - Assemble into a `ThemeCollection`.
  - [ ] 2.2: Handle missing files gracefully — a missing `tokens/` directory (first-ever `update` after `init`) means no prior state; return an empty collection. All tokens will appear as "added" in the diff.

- [ ] **Task 3: Diff renderer — `renderTokenDiff` (AC: #4, #5, #6, #7, #8, #9, #10, #11)**
  - [ ] 3.1: In `src/ui/diff.ts`, export `renderTokenDiff(diff: TokenDiff, primitives: PrimitiveToken[]): void`. The `primitives` param is the current (post-pipeline) primitives, used for resolving color hex values for swatch display.
  - [ ] 3.2: **Primitive changes section.** Group `diff.primitiveChanges` by `category`, then within color by hue (extract from `path[1]`). For each change:
    - `added`: `+ name  value` (green swatch for colors)
    - `removed`: `− name  value` (red-tinted or dimmed swatch for colors)
    - `modified`: `name  oldValue → newValue` (swatches for both old and new if color)
    Use `p.log.step("Primitive Changes — Color")`, `p.log.info(lines)`.
  - [ ] 3.3: **Semantic changes section.** Iterate `diff.semanticChanges` by theme. Group within each theme by category. Same `+`/`−`/`→` formatting. For color semantics, resolve hex from `primitives` for swatch display via the existing `resolveHex` helper (extract from `src/ui/preview.ts` if it's not already exported, or duplicate the small helper in diff.ts).
  - [ ] 3.4: Use `supportsColor()` from `src/utils/color-display.ts` to gate swatch rendering.
  - [ ] 3.5: Emit nothing if the diff section is empty (e.g., no semantic changes → skip the semantic section entirely).

- [ ] **Task 4: Cascade summary — `renderCascadeSummary` (AC: #12, #13)**
  - [ ] 4.1: In `src/ui/diff.ts`, export `renderCascadeSummary(diff: TokenDiff, current: ThemeCollection): void`.
  - [ ] 4.2: Count the number of changed primitives and affected semantic tokens. A semantic token is "affected" if its change corresponds to a modified primitive (match by checking if the semantic's `$value` references a changed primitive path). Emit: `"Changing N <category> primitives affects M semantic tokens across K themes."` via `p.log.info`.
  - [ ] 4.3: For removed primitives, check if any semantic token in the current collection still references the removed primitive's path. If so, emit a warning per broken reference: `p.log.warn("⚠ <semantic.name> references removed primitive {<path>}")`.
  - [ ] 4.4: If no cascading impact (only direct primitive changes, no semantic ripple), emit: `"No cascading semantic changes."`.

- [ ] **Task 5: Post-diff prompt flow (AC: #14, #15, #16, #17, #18)**
  - [ ] 5.1: In `src/commands/update.ts`, after `renderTokenDiff` and `renderCascadeSummary`, present a `p.select`:
    ```typescript
    const action = await p.select({
      message: "What would you like to do?",
      options: [
        { value: "accept", label: "Accept changes and write", hint: "Write modified token files and rebuild CSS" },
        { value: "preview", label: "Review full token preview", hint: "See all tokens (not just changes) with the override editor" },
        { value: "back", label: "Go back and modify further", hint: "Return to category picker" },
        { value: "cancel", label: "Cancel", hint: "Exit without writing" },
      ],
    });
    ```
  - [ ] 5.2: `"accept"` → skip `previewAndConfirm`, proceed directly to `runOutputGeneration` with the scoped `WriteScope`. Apply overrides via `applyPriorOverrides` before writing.
  - [ ] 5.3: `"preview"` → enter `previewAndConfirm(collection, { initialOverrides })` (existing flow). If the user accepts from the preview, proceed to write. If cancelled, return to the post-diff prompt.
  - [ ] 5.4: `"back"` → loop back to `collectUpdateInputs`. Re-run the pipeline. Compute a new diff. Display the new diff. Present the post-diff prompt again.
  - [ ] 5.5: `"cancel"` → `p.cancel("No changes written.")` + return.
  - [ ] 5.6: Wrap in a loop so the user can toggle between diff → preview → back → diff any number of times until they accept or cancel.

- [ ] **Task 6: Wire into `updateCommand` and handle no-changes (AC: #3, #19, #20, #21)**
  - [ ] 6.1: In `src/commands/update.ts`, after `runUpdate` returns the pipeline result and override conflicts are resolved:
    1. Load prior collection via `loadPriorCollection(config, cwd)`.
    2. Compute diff via `computeTokenDiff(priorCollection, pipelineResult.collection)`.
    3. If `diff.isEmpty` → `p.log.info("No changes to apply — your token system is up to date.")` + `p.outro` + return.
    4. Render diff via `renderTokenDiff(diff, pipelineResult.collection.primitives)`.
    5. Render cascade summary via `renderCascadeSummary(diff, pipelineResult.collection)`.
    6. If component tokens exist in the collection → `p.log.info("Component tokens are unchanged — they reference semantic tokens and will inherit your changes via the CSS cascade.")`.
    7. Enter the post-diff prompt loop (Task 5).
  - [ ] 6.2: The existing `previewAndConfirm` call in `updateCommand` (Story 3.1) is replaced by the new post-diff prompt. The full preview is still available as an option within the prompt.

- [ ] **Task 7: Tests (AC: all)**
  - [ ] 7.1: `src/ui/__tests__/diff.test.ts` — pure diff engine tests:
    - No changes (identical collections) → `isEmpty === true`.
    - Added primitive (new hue ramp) → `kind === "added"`, correct path + value.
    - Removed primitive (dropped hue ramp) → `kind === "removed"`, correct old value.
    - Modified primitive (changed hex) → `kind === "modified"`, old + new values.
    - Added/removed/modified semantics, per-theme.
    - Mixed: primitives + semantics changes in single diff.
  - [ ] 7.2: `src/ui/__tests__/diff.test.ts` — cascade summary tests:
    - N primitives → M semantic tokens affected.
    - Removed primitive with dangling semantic reference → warning emitted.
    - No cascade when only non-referenced primitives changed.
  - [ ] 7.3: `src/ui/__tests__/diff.test.ts` — renderer tests (mock `@clack/prompts`):
    - Color primitive diff renders old + new swatches (when color enabled).
    - Non-color primitive diff renders old → new values.
    - Semantic diff renders per-theme grouping.
    - Empty sections are skipped (no "Semantic Changes" header when only primitives changed).
  - [ ] 7.4: `src/pipeline/__tests__/diff-loader.test.ts` — prior collection loading:
    - Round-trip: write tokens to tmp dir, load back, compare.
    - Missing tokens directory → empty collection.
    - Partial on-disk state (some categories present, others missing).
  - [ ] 7.5: `src/commands/__tests__/update.test.ts` — extend with:
    - No-changes early exit (mock pipeline to return identical collection).
    - "Accept changes and write" path (skips preview).
    - "Review full token preview" path (enters preview, then writes).
    - "Go back and modify further" path (loops back to category picker).
    - "Cancel" path (no writes).
  - [ ] 7.6: `npm run type-check`, `npm test`, `npm run build`, `npm run validate:sprint` — all clean.

- [ ] **Task 8: Close-out**
  - [ ] 8.1: Update `src/commands/update.ts` outro / help text to mention the diff display.
  - [ ] 8.2: Update README.md "Updating your token system" section (added in Story 3.1) to mention the diff display behavior.
  - [ ] 8.3: Move this story to `review`, then to `done` after code review.

## Dev Notes

### Relevant ADRs

- **[ADR-001](../architecture/adr-001-non-destructive-json-merge.md)** — Per-category file ownership. The diff loads the prior state from per-category files on disk, then compares against the regenerated collection. The per-file layout makes it straightforward to load only what's needed.
- **[ADR-002](../architecture/adr-002-story-status-single-source-of-truth.md)** — `npm run validate:sprint` must pass after every status transition.

### Previous Story Intelligence (Story 3.1)

Story 3.1 shipped the `update` command with selective regeneration and `loadPrimitivesFromDisk` / `loadSemanticTokensFromDisk` / `loadComponentTokensFromDisk` in `src/pipeline/load-from-disk.ts`. These loaders reconstruct `PrimitiveToken[]`, `Map<string, SemanticToken[]>`, and `ComponentToken[]` from the on-disk DTCG JSON files using a `walkDtcgLeaves()` callback pattern.

Key patterns from Story 3.1 to build on:
- `updateCommand` (in `src/commands/update.ts`) orchestrates: load config → collect inputs → run pipeline → resolve override conflicts → preview → write. The diff display inserts between "resolve conflicts" and "preview".
- `UpdatePipelineResult` contains `{ collection: ThemeCollection; modifiedCategories: readonly string[] }`. The `modifiedCategories` list determines which categories are interesting for diffing (AC #20: only show changed categories).
- `previewAndConfirm` (in `src/ui/preview.ts`) renders the full collection and offers the override loop. Story 3.2 makes the full preview an opt-in step rather than the default — the diff is the new default view for `update`.
- `src/utils/color-display.ts` exports `hexToAnsi(hex)` and `supportsColor()` — reuse for diff color swatches.

### Diff Architecture

```
Prior collection (from disk)     Current collection (from pipeline)
         │                                    │
         └──────────┬─────────────────────────┘
                    │
           computeTokenDiff()
                    │
              TokenDiff {
                primitiveChanges: TokenChange[]
                semanticChanges: Map<themeName, TokenChange[]>
                isEmpty: boolean
              }
                    │
         ┌──────────┼──────────┐
         │          │          │
  renderTokenDiff  renderCascade  (no changes → exit)
         │          │
         └──────────┘
              │
    Post-diff p.select prompt
    ┌────┬────┬────┬────┐
  Accept Preview Back Cancel
```

### Technical Constraints

- **Diff engine is pure.** `computeTokenDiff` takes two `ThemeCollection`s and returns a `TokenDiff`. No I/O, no Clack. Trivially testable.
- **Renderer is Clack-based.** `renderTokenDiff` and `renderCascadeSummary` call `p.log.step`, `p.log.info`, `p.log.warn`. Tested by mocking `@clack/prompts` (same pattern as `src/ui/__tests__/preview.test.ts`).
- **Prior collection loading reuses Story 3.1 loaders.** The `loadPriorCollection` helper in `src/pipeline/diff-loader.ts` is a thin composition of the existing `loadPrimitivesFromDisk`, `loadSemanticTokensFromDisk`, and `loadComponentTokensFromDisk`.
- **Scoping diff to modified categories.** While `computeTokenDiff` compares full collections, `renderTokenDiff` can filter its output to only show changes in `modifiedCategories`. This keeps the display focused — the user doesn't see noise from categories they didn't touch (whose loaded-from-disk values should be identical to the regenerated values).

### Diff Display Format

Example terminal output:

```
◆  Primitive Changes — Color

  blue (3 modified)
    color.blue.400  ██ #60a5fa → ██ #3b82f6
    color.blue.500  ██ #2563eb → ██ #1d4ed8
    color.blue.600  ██ #1d4ed8 → ██ #1e40af

  + teal (10 added)
    + color.teal.50   ██ #f0fdfa
    + color.teal.100  ██ #ccfbf1
    ...

  − accent (10 removed)
    − color.accent.50   ██ #fdf4ff
    ...

◆  Semantic Changes (Default)

  Color
    color.background.primary  → {color.blue.500} → {color.blue.600}
    + color.background.accent  → {color.teal.500}
    − color.background.info    → {color.accent.500}

◇  Changing 13 color primitives affects 8 semantic tokens across 2 themes.
◇  ⚠ color.background.info references removed primitive {color.accent.500}
◇  Component tokens are unchanged — they reference semantic tokens and will inherit
   your changes via the CSS cascade.

◆  What would you like to do?
   ● Accept changes and write
   ○ Review full token preview
   ○ Go back and modify further
   ○ Cancel
```

### Technical Stack

| Concern | Choice | Version |
|---------|--------|---------|
| Runtime | Node.js | >=18 (LTS) |
| Language | TypeScript | ^5.x |
| Build | tsup | ^8.x |
| CLI prompts | `@clack/prompts` | ^1.2.0 |
| Test runner | Vitest | ^4.x |
| Output transforms | Style Dictionary | ^5.4.0 |
| Color engine | `@quieto/engine` | ^0.1.1 |

### What NOT to Build

- **Do NOT implement `--dry-run`.** That is Story 3.3.
- **Do NOT implement changelog generation.** That is Story 3.4. The diff is terminal-only.
- **Do NOT add diff display to `init`, `add`, or `component`.** Story 3.2 scopes the diff to `update` only. Other commands continue to use the full preview. Extending diff to other commands can be revisited in a future story.
- **Do NOT implement interactive per-token accept/reject.** The user accepts or rejects the whole change set. Fine-grained overrides are available through the existing `previewAndConfirm` override flow.
- **Do NOT diff component tokens.** Components are pure references, not regenerated by `update`. The note about CSS cascade inheritance is sufficient.
- **Do NOT diff CSS output.** The diff operates at the DTCG token level, not at the CSS custom property level. CSS is a build artifact rebuilt from tokens.
- **Do NOT persist the diff.** The diff is a terminal display. Persistence is Story 3.4 (changelog).
- **Do NOT migrate `compareVersions`, extract the shared version resolver, or add lockfile protection.** All still deferred per `docs/planning/deferred-work.md`.

### File Structure (final target)

```
src/
├── cli.ts                            ← unchanged
├── commands/
│   ├── update.ts                     ← modified (diff integration, post-diff prompt loop)
│   └── ...                           ← unchanged
├── pipeline/
│   ├── diff-loader.ts                ← NEW (loadPriorCollection helper)
│   ├── load-from-disk.ts             ← unchanged (reused by diff-loader)
│   ├── update.ts                     ← unchanged
│   └── ...                           ← unchanged
├── ui/
│   ├── diff.ts                       ← NEW (computeTokenDiff, renderTokenDiff, renderCascadeSummary)
│   ├── preview.ts                    ← unchanged (may extract resolveHex if needed)
│   └── __tests__/
│       ├── diff.test.ts              ← NEW
│       └── preview.test.ts           ← unchanged
├── utils/
│   └── color-display.ts              ← unchanged (reused for swatches)
```

### References

- [Source: docs/planning/epics.md#Story 3.2: Token Diff Display]
- [Source: docs/planning/stories/3-1-re-entrant-editing.md] — update command, selective regeneration, load-from-disk helpers, UpdatePipelineResult
- [Source: src/ui/preview.ts#previewAndConfirm] — existing preview flow (lines 341–359), renderPreview (lines 152–162), color formatting helpers
- [Source: src/ui/preview.ts#resolveHex] — private helper for resolving DTCG color refs to hex (lines 12–23) — consider extracting to a shared util or duplicating in diff.ts
- [Source: src/pipeline/load-from-disk.ts] — loadPrimitivesFromDisk, loadSemanticTokensFromDisk, loadComponentTokensFromDisk, walkDtcgLeaves
- [Source: src/commands/update.ts] — update command orchestrator, data flow between pipeline and preview
- [Source: src/utils/color-display.ts#hexToAnsi] — ANSI swatch rendering (lines 18–21)
- [Source: src/utils/color-display.ts#supportsColor] — terminal color support detection (lines 30–44)
- [Source: docs/planning/architecture/adr-001-non-destructive-json-merge.md] — per-category file layout enabling diff loading
- [Source: docs/planning/deferred-work.md] — deferred items that remain out of scope

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
